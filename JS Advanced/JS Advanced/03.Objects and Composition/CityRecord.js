function Solve(name, population, treasury) {
    const City = {
        name,
        population,
        treasury,
    };

    return City;
};

Solve('Tortuga', 7000, 15000);
Solve('Santo Domingo', 12000, 23500);
